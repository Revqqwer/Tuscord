{
  "name": "tuscord",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run dev --workspace=@tuscord/server & npm run dev --workspace=@tuscord/web",
    "build": "npm run build --workspace=@tuscord/shared && npm run build --workspace=@tuscord/server && npm run build --workspace=@tuscord/web",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:generate": "npm run db:generate --workspace=@tuscord/server",
    "db:migrate": "npm run db:migrate --workspace=@tuscord/server"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
